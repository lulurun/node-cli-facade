/* jshint newcap: false */
/* global F */
(function() {
  'use strict';

  var config = {
    Requires: [
      'https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'
    ],
    Envs: {
      facade: '/facade/index.js',
    }
  };
  F.construct(config);

})();

